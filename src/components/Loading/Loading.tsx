import React from "react";
import "./Loading.css";

type Props = {};

const Loading = (props: Props) => {
  return (
    <div className="dots">
      <span></span>
    </div>
  );
};

export default Loading;
